services:
  itverse-https:
    image: nginx:alpine
    container_name: itverse-https
    ports:
      - "8443:443"
    volumes:
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./docker/certs:/etc/nginx/certs:ro
    depends_on:
      - itverse-web
